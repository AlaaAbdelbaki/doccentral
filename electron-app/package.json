{
  "name": "doccentral-desktop",
  "productName": "DocCentral",
  "version": "0.1.0",
  "description": "DocCentral desktop prototype — Patients page (Electron, faithful to the claude.ai/design DentaCore Patients design)",
  "main": "main.js",
  "private": true,
  "scripts": {
    "build": "esbuild src/index.jsx --bundle --outfile=dist/bundle.js --jsx=automatic",
    "start": "npm run build && electron ."
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "electron": "^33.2.0",
    "esbuild": "^0.24.0"
  }
}
